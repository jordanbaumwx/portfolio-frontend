import React from 'react';
import Bio from '../Bio';
import SkillBadge from '../SkillBadge';

const Hero = () => {
  return (
    <section className="">
      <Bio />
    </section>
  );
};

export default Hero;
